con N : Int { 1 ≤ N }
con F : array [0..N) of Int
var r, n : Int

{:
  sum p q = ⟨ Σ i : p ≤ i < q : F[i] ⟩

  Q  = 0 ≤ n ≤ N
:}

  -- Write down a specification such that
  -- r is the maximum sum of non-empty segments.

{ r = ??? }
